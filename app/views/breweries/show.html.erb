<p id="notice"><%= notice %></p>

<h2><%= @brewery.name %>
  <% unless @brewery.active %>
    <span class="label">retired</span>
  <% end %>
</h2>

<p id="established">Established year <%= @brewery.year %></p>

<p>Number of beers <%= @brewery.beers.count %></p>

<ul>
<% @brewery.beers.each do |beer| %>
  <li><%= link_to beer.name, beer %></li>
<% end %>
</ul>

<p>
<% if @brewery.ratings.empty? %>
  beers of the brewery have not yet been rated!
<% else %>
  beers of the brewery have <%= pluralize @brewery.ratings.count, 'rating' %>, average <%= @brewery.average_rating %>
<% end %>
</p>

<% if current_user %>
  <%= link_to 'Edit', edit_brewery_path(@brewery) %>
  <%= link_to('Destroy', @brewery, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger') if current_user.admin %>
<% end %>
